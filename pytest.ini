[pytest]
testpaths = tests

addopts =
    -v
    --tb=short
    --no-header
    -rN
    --durations=5

markers =
    unit: Pure unit tests — no I/O, no external services
    integration: Tests that call the FastAPI app (mocked dependencies)
    slow: Tests that take more than a second